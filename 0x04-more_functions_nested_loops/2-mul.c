#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: character to be multplied
 * @b: character to be multplied
 * Return: 0
 */

int mul(int a, int b)
{
	return (a * b);
}
